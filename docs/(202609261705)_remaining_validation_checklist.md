# AD-SDIRK3 remaining validation after U composite parity and cfn refresh

Local timestamp: 2026-09-26 17:05:02 JST (+0900)

This updates the checklist in `(202609261003)_remaining_validation_checklist.md` for a draft source candidate stacked on PR #240. The candidate adds one bounded production fix and source-extracted U diffusion tests; it does not merge the draft stack or establish operational forecast/data-assimilation acceptance.

| ID | Current status | Evidence and next closure condition |
|---|---|---|
| I0 integration | **Open** | Draft PRs #233–#240 are stacked, with exact-head CI for #239 and #240. This U coefficient candidate needs its own exact-head CI and final combined clean WRF build before any merge decision. |
| F1 option-1 scalar/momentum | **Scoped candidate passed** | Existing option-1 source and derivative contracts were unchanged by the cfn refresh, but the shared top deformation coefficient can affect both options; retain option-1 regression and broader boundary/MPI/TLAD work. |
| F1 option-2 scalar | **Scoped candidate passed** | Fixed constant-K stage metric tests pass. Dynamic/supplied K, moist states and noncanonical tile options remain unsupported or unverified. |
| F1 option-2 U | **Composite operator parity for one fixture; full RHS open** | An exact source-extracted Fortran chain produces metric, deformation, vertical and horizontal U stress tendencies, then `rk_addtend_dry` U normalization on one common terrain state. The C++ composite of the corresponding private helpers matches all owned U levels for interior and top pulses at FP32/REAL64 O0/O2. Distinct khdif/kvdif and wrong-K/stale-stage-metric controls discriminate important omissions. This is not a `computeUnifiedRHS`-through-WRF-halo comparison. V/W/scalar combined RHS parity, other coefficients/maps/BC/tile layouts remain open. |
| F1 top extrapolation `cfn/cfn1` | **Bounded production defect fixed in candidate** | With externally supplied WRF `fnm/fnp`, a two-call zero-copy test changes top `rdnw` from 4 to 2 and verifies `cfn/cfn1` changes 1.5/−0.5→2/−1 while weights remain unchanged. The old max-of-independent-epochs gate missed this update. New code refreshes only top coefficients each WRF call; external→internal weight-source switching is a separate unverified lifecycle. |
| F1 option-2 vertical U/V/W | **Prior bounded defect closure maintained** | Source-extracted U/V/W stress oracles from PR #239 pass on their prescribed states. Full coupled diffusion parity remains open. |
| P1 one-step active wind blow-up | **Resolved in tested dry case** | PR #239 stage-metric correction removed the 29,807 m/s U RMS blow-up. This candidate's corrected candidate-archive relink one-step PC2/RK3 outputs match the #239 clean reference byte-for-byte in the static 17³ case; that does not exercise a changing eta metric in full WRF. |
| S1b physical budgets | **Scoped existing coverage; quantitative endpoint closure open** | Nonunit area, hybrid layer mass, accepted-stage face/source checks and replay already exist. A candidate ARK bilinear identity experiment closed algebraically, but its omitted-term mutants were only ~2.07× the experiment bound; no weak assertion was retained. Need a better-resolved fixture or justified endpoint product-defect budget. |
| Reproducible full WRF | **Closed for exact PR #239 macOS setup; current candidate uses relink** | Fresh #239 checkout built ideal/wrf and regenerated the archived input byte-identically. This new cfn candidate was Make-rebuilt and linked against those unchanged Fortran objects; exact new-source clean full rebuild remains open. |
| G1 decomposition | **Open** | Need two tile layouts and MPI halo/owner equivalence for forward and transpose. |
| K1 coefficient derivatives | **Open** | Fixed-K RHS derivatives exist; diagnosed K/rho policy and variable-coefficient stage JVP/VJP remain. |
| T1 time accuracy, stability, cost | **Open** | Clean #239 240-second PC2/RK3 dt60/30/15 runs are finite but have mixed/nonmonotone self-differences. Tight PC2 dt15 with 1e−7 tolerances failed near a 1.3e−6 scaled residual floor. No third-order or equal-accuracy speed claim. |
| A1 complete active-step adjoint | **Open** | Need the actual ARK-step derivative with current geometry/density, packing, physical boundaries/MPI transpose and objective Taylor test; WRFPLUS TL/AD must match current primal. |

Next smallest steps: run exact-head CI for this source candidate, then compare a fixed common state through the complete option-2 U/V/W/scalar diffusion RHS with actual WRF halo/BC and map/mass coupling. Resolve any first mismatch rather than selecting only passing states. The present composite U fixture uses analytically prepared halo and a helper composition; it is not evidence for MPI communication or full-step adjoint correctness. The source/test report records the exact Fortran and C++ file hashes, local 102/102 CTest, Make archive contract and corrected relink receipt. No full clean WRF build or RK3 multi-step comparison was performed by the cfn change itself.
