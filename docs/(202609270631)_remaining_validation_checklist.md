# Remaining AD-SDIRK3 validation checklist after the fixed-mass U correction

Local timestamp: 2026-09-27 06:31:08 JST (+0900)

Context: the code tree at `abc4db9e2a70eec0ed5299f3577cea4698395062` is stacked on draft PR #246 (`427b1cf`). The candidate changes option-2 U stress diffusion's physical-rate conversion and adds map-1/map-1.25 source-equation contracts. It restores the #246 stage-history header, so that unrelated diagnostic change is absent from the final diff. The parallel Fortran-oracle compile race is fixed with a private working directory. This checklist scopes what the evidence closes and what remains; the final integrated artifact hashes are in the companion validation addendum.

| Item | Current evidence and status | Next closure condition |
|---|---|---|
| I0 exact integration | **Local candidate validated; draft PR and exact-head CI pending.** Post-revert parallel CTest passed 106/106. The Make archive has exact 24-object parity and a fresh candidate-first WRF relink completed. | Push the exact integrated tree, run CI at its HEAD, and verify the source/executable/input receipts in the PR. |
| F1 option-2 U stress physical rate | **Closed only for fixed mass and the declared owned U cells.** The actual RHS ON−OFF rate matches source-extracted Fortran `D/L_s` within `1.31e-11` (map 1) and `1.20e-11` (map 1.25), against a `1e-9` FP32 budget. The old conversion misses by `2.34e-7` and `2.48e-7`. | Compare later ARK/small-step states with distinct `L_s` and `L_t`; verify physical BC/halo, supplied coefficients, and materially nonunit-map WRF activation. |
| F1 option-2 V/W actual RHS | **Open.** Source-extracted local composite tests exist, but physical-basis actual-RHS probes retain residuals. The V residual is not explained by a simple H/Z normalization formula; W's direct helper did not use exact production rho/metric/mass operands. | Capture the effective Step 9/10 operands and first differing raw contribution at actual call sites; change a production formula only after the first mismatch is established. |
| F1 scalar/other options | **Bounded scalar source-equation contracts remain valid.** Option-1 smoke, dynamic/supplied coefficients, spatially variable rho/K, general map/terrain/BC combinations, and full U/V/W/stress parity are not closed by this U change. | Use same-state source-equivalent comparisons with matching location, unit, and coefficient ownership. |
| S1b dry mass-weighted theta endpoint budget | **Existing accepted-stage flux/source checks remain.** A separate test-only factored endpoint product identity passes with a 440x bilinear-omission mutant separation on the original closed fixture; it is not yet integrated into this candidate. | Integrate that reviewed test on the final stack, rerun the affected/full checks, and keep the physical stage receipts distinct from the algebraic identity. |
| G1 domain decomposition | **Open.** Whole-step multi-tile/MPI and transpose equivalence are not established; unsupported layouts must remain fail-closed. | Compare assembled forward and transpose owners/halos with one-domain results under the same physical boundary. |
| K1 active coefficient derivatives | **Open.** Fixed-K differentiation does not establish derivatives through diagnosed rho, geometry, or K. | Declare which coefficients vary with each RHS state and verify stage JVP/VJP accordingly. |
| T1 temporal accuracy/stability | **Open.** The earlier refinement ladder was nonmonotone, and the strict Stage-2 case still stalls with the same decision when snapshots are enabled. | Resolve the failing-state RHS/replay question, then measure solution error under timestep refinement with solver error below the temporal signal and compare equal-accuracy cost. |
| A1 whole-step active adjoint | **Open.** A single-tile active-step pullback exists; physical boundary, packing, MPI transpose, and full objective Taylor/WRFPLUS agreement remain. | Verify the actual full step, including declared coefficient dependencies and boundary transforms. |

The final candidate-first `test/em_b_wave` one-step check used the archived same-setup split-explicit RK3 reference: one rank/thread, 60 s, `diff_opt=2`, `khdif=1000`, `kvdif=10`. Candidate PC2 and RK3 outputs are byte-identical to the matching clean #246 outputs; candidate PC2 K=0 is also identical to its #246 control. All three runs reached `SUCCESS COMPLETE WRF` and their floating output fields are finite. The candidate PC2-minus-RK3 endpoint RMS differences include U `0.00209012`, V `2.21139e-5`, W `0.000665487`, PH `0.306797`, T `0.0103998`, P `0.0398132`, and MU `0.0415027` in each variable's output unit. This is a one-step regression/comparison, not a forecast-accuracy, time-order, or speed acceptance result. The relink reused clean #246 Fortran objects and is not a new full clean WRF build of this candidate.

Next action: open the draft PR and run exact-head CI, then integrate the S1b identity as a separate test-only change. Diagnose V/W with effective same-call operands before applying any normalizing factor. Preserve the open G1/K1/T1/A1 items rather than treating this bounded U closure as complete solver qualification.
