# Governing-equation review checklist

Timestamp: 2026-09-24 15:34:18 JST (+0900)

Baseline: `main` d9917310c15906f8cf555688dcd6189d0172526a. Candidate PR #215 HEAD 91f7c4e459e6125ae3a07297a80e5e486fe67cbf, tree 45cfa6a3318882fbd2add68c46f31a19e6e849c4; automatic CI run 35957697306 passed all four jobs, 101/102 CTests passed with one MPS skip. The PR remains open. Production implementation SHA-256 b52c2726b38ef6d8d18a6090cecdcf02dad2a3e1aa2083a69b63736fcf8f12e0.

Statuses describe the stated scope only. A candidate-branch test is not evidence that `main` contains it. Work down this list one item at a time; update a row only with an exact source and execution receipt. The first currently actionable item is H1.

| ID | Finding / required result | Status | Closure evidence |
|---|---|---|---|
| I0 | Previously feature-only governing-step content reaches the source actually used as `main`. | **Partly closed** | PR #215 merged to `main` as `7f47499`; its tree matches the reviewed #215 source, so its contents are on `main`. PRs #216/#217 were merged only into feature branches; their main integration is open PR #218. Do not mark H1/S1a present on `main` until that integration tree lands. |
| D1 | Whole-step inner product can mask the small active diffusion derivative. Test `D(G_K-G_0)` directly with a resolved signal and a stated absolute floor. | **Closed for the dry fixed-K fixture on `main`** | PR #215 `Full_Tile_Step_Adjoint`: U Fourier increment AD −7.79684e−6; h=.4/.2 FD errors 2.30e−7/1.11e−7 below engineering budgets 1.55e−6/2.33e−6. Downloaded CI artifact confirms records; limits of the output-roundoff floor are stated. This is not full-model adjoint qualification. |
| B1 | Budget uses mass-cell physical area rather than a uniform area sum. | **Closed for the smooth-map sigma fixture on `main`** | PR #215 smooth nonunit-map sigma case: weighted dry-mass drift .0178 kg versus 14.94 kg budget; naive unit-area drift 37131 kg. Weighted theta content and anomaly are also measured with the 300 K mass term separated. Same-source CI artifact confirms. This does not close H1. |
| H1 | Replace the current uniform-eta, `c1h=1,c2h=0` assumption with a positive, hydrostatically coherent **nonuniform eta and genuinely variable hybrid coefficients** in the same tile budget. | **Candidate verified** | The extended `Full_Tile_Step_Adjoint` uses WRF `hybrid_opt=2` coefficients at `etac=0.2`, eta faces `[1,.9,.7,.4,0]`, the existing smooth nonunit map, and an EOS/hydrostatic base. It checks positive layer masses, column mass normalization, an independent initial theta sum, one completed step's physical mass/theta closure, and a compensated layer transfer for which physical theta change is zero but uniform-/nz weighting is wrong. Local Clang 22.1.4/libtorch test passed; exact values and hashes below. Close for this fixed-K dry single-tile case after its stacked PR is integrated. |
| S1a | Observe accepted ARK stage states/rates and separate stage weighted net rates, raw ARK update, and final projection in one nonuniform closed tile. | **Candidate verified** | The default-off trace captures four accepted stage states and fast/slow/full derivatives; a fixed-K dry hybrid test checks physical area/layer weights, fast MU against the production mass-flux helper, zero external tendency and closed mass boundaries, weighted mass/theta stage rates, exact raw ARK replay, and final projection/writeback. Local tests and archived-object WRF regression are in the [17:17 progress report](./(202609241717)_ark_stage_budget_trace.md). This is not a full physical face/source decomposition. |
| S1b | Connect each ARK stage's **theta and momentum physical face fluxes and internal/external sources** to the final drift on supported physical boundaries. | **Open** | Capture the actual operator-produced face fluxes/sources and their owned-face signs, separate Newton defect from RHS rate, and account for the nonlinear mass×theta ARK product defect. Extend beyond the closed single-tile fixture only with explicit halo/physical-boundary ownership. Do not infer fluxes from a small final drift. |
| F1 | Same-state original Fortran operator comparison for active diffusion. | **Open** | Begin with one fixed-K dry horizontal scalar/theta diffusion kernel on identical PH/PHB, rho, deformation, map factors, coefficients, halo/BC and owned cells; compare raw tendencies in the same units. Extend to U/V/W only after each producer/consumer location and normalization is fixed. A full U/V/W/scalar RHS comparison needs a dedicated stage capture path. |
| K1 | Derivative policy for diagnosed `K(U)` is declared and tested. | **Open** | State whether a coefficient is frozen external input or diagnosed from U; if diagnosed, compare its contribution to the full RHS VJP/FD. Fixed-K passes do not close this item. |
| G1 | Boundary/halo and domain-split generalization, including outer corner coefficient ownership and V 1D fallback. | **Open** | Verify physical boundaries versus MPI halos; same physical domain with different tile partitions and interior-face flux cancellation. Only treat the U corner K/2 endpoint as an output defect if a supported consumer reads it. |
| T1 | Active-case time order, stability, and same-accuracy RK3/PC0/PC2 comparisons. | **Open** | Use a physically consistent source/executable/input configuration, multiple timesteps to one end time, tighter Newton control, variable-specific field errors and repeated runtime. The archived four-step flat `em_b_wave` comparison is context, not this closure. |
| A1 | Actual whole-WRF/observation-path adjoint and assimilation suitability. | **Open** | Differentiate the executed full step including packing, BC, halo, physical source policies and observation path; perform objective Taylor/dot checks in supported configurations. The single-tile pullback is a narrower contract. |
| O1 | Existing `stage_operand_diag` closure on the new hybrid fixture. | **Candidate verified in O1 branch** | The original run aborted at `hist_rel=1.523801e-6`, `hist_max_abs=3.649131e-7`. The diagnostic replay now forms the double ARK scalar before applying it to FP32 tensors and uses bit-exact FP32 endpoint replay when ideal FP64 increment summation exceeds the fixed ratio gate; the separate per-source replay remains strict. A synthetic rounded-history positive case and wrong-sign negative controls pass, the hybrid step with the diagnostic enabled completes, and the focused CTests pass 2/2. Exact receipt: [18:15 report](./(202609241815)_stage_operand_rounding.md). Do not close on `main` until this change is integrated. |

H1 local receipt (2026-09-24 15:48 JST): the isolated worktree started at PR #215 HEAD `91f7c4e`. Source `test_full_tile_step.cpp` SHA-256 `7592faf203691df640dffada753ef3a3aecd292aba32722bfe9e65720d529d67`; executable SHA-256 `f13fb737724e4d5de207e1e103b3774785d4715fbcfc4fdf892f661fa7b529c2`. CMake/Ninja used Apple Clang 22.1.4 and Homebrew libtorch; `ctest -R '^Full_Tile_Step_Adjoint$'` passed 1/1 in 21.20 s. `GOV_HYBRID` reported physical dry-mass drift 0.0443 kg (FP32 budget 14.94 kg), mass-weighted theta drift 1.89546e4 kg K (budget 9.44758e5 kg K), and a synthetic packed-state transfer with zero physical theta error but 1.39815e9 kg K error under uniform-/nz weighting. The actual step's uniform-weight drift was 1.93796e4 kg K; that number is diagnostic, not a claim that this particular step would fail the same tolerance under uniform weights. The documented error budgets apply to this fixture and are not general error bounds. Green/Red source reviews corrected a horizontal/vertical weighting confound, a signed anomaly error scale, and added independent total checks before this receipt.

For the earlier H1-only PR #216, no new WRF `em_b_wave` run or same-setup RK3 field/runtime comparison was performed: its production implementation was unchanged. The later S1a candidate **did** relink archived WRF objects and repeat short K0/active-diffusion runs, as recorded in the 17:17 report; it did not perform a fresh full WRF rebuild or new RK3 run. Graphify inspected a source-hash-matched five-file corpus before the H1 edit and was refreshed after it; its parser does not establish numerical relationships, so the WRF formulas and caller inputs were verified from the authoritative files.

17:17 JST update: S1a has a reviewed local implementation and model regression receipt in [`(202609241717)_ark_stage_budget_trace.md`](./(202609241717)_ark_stage_budget_trace.md). S1b and O1 remain open. PR #215 and #216 remain separate open integration prerequisites; a candidate stage-budget PR does not mark them present on `main`.

18:15 JST update: PRs #215/#216/#217 have since been merged, but only #215 targeted `main`; #218 is the open main integration for H1/S1a. O1's exact FP32 replay fix is separately verified in the candidate branch, while S1b, F1, K1, G1, T1 and A1 remain open. The earlier 17:17 sentence above is retained as the historical state of that report, not the current PR state.

19:12 JST update: the [current remaining-items checklist and theta seam progress](./(202609241912)_remaining_findings_theta_seam.md) supersede the statuses above. It separates the candidate stage-1 theta face check from still-open all-stage theta, momentum, Fortran parity and domain-split contracts.
