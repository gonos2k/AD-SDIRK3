# Integration candidate against current main

Timestamp: 2026-09-25 14:54:06 JST (+0900)

## Context and merge

Remote `main` remained `27abe61b12337c0192c7dfa40f6f1184e1f8e46b` during this integration check. The option-1/option-2 feature stack through test PR #232 was merged into an isolated branch without conflicts, followed by the separate option-1 active momentum VJP commit from PR #228. The integration branch HEAD is `8e387774c83f757821f5c246aad9f7aecdcf7c33`; both active option-1 and option-2 scalar/momentum tests are present in the final test executable. No remote `main` merge was performed. The focused Graphify snapshot was verified on `main` before merging and refreshed after the integration tree to 754 nodes, 1,564 edges and 28 communities; source/test copies matched their worktree hashes.

## Validation

The integrated Apple Clang/Homebrew libtorch CMake core built successfully. The separate production Make archive was then built from the same source and passed the exact 24-member manifest check; its SHA-256 is `e35dc5cc9e9f5f42a08075288e3e467f675b60420b7d3063c9cee5b35a35447c`. The first full CTest invocation failed only because it preceded this required Make archive (`Core_Archive_MakeParity` reported archive absent). After building it, a fresh full CTest invocation passed **102/102**, log SHA-256 `8b07e925503f53b0252cc9ca73a2fda40d4ee75fba2a8bcbdb7b5920d45e3104`. Local `actionlint` and the repository's from_blob/item-guard ratchets passed.

The integrated scalar contract binary directly ran both option-1 and option-2 active diffusion increment VJP tests and passed (log SHA-256 `80c0de5a31e947ff54ca4161546b08a5ae0626e35d31b53a4522ace2284b929b`). Source-extracted Fortran/C++ option-1 momentum, hybrid option-1 scalar and option-2 scalar including variable K/rho/nonunit-map comparisons all passed. Their local log SHA-256s are respectively `aa7e43a956f8fd297736ab3495a9e1b0097b1dc2bf6f808b5ee44109e955a033`, `52bb12e4498f3fc2a6f7d9dda951e7023816384f74b000b3e26b81e9f4077f1a`, and `80c0174d0333bd07feef49ff52096ccd28346d17db7e3089f3f6281f6e07c4e4`.

The integration tree's production C++ and modified Fortran source blobs have exactly the same SHA-256 as the bounded #230 validation source (`8b492ff72b379fac5924b2bb79978429e3742eec5795fbc0aa19ca3f427c16f0` and `231ee33571b8f7ed9020bb3bb568665674ccc00d040d064e9d830107550284bd`). They were incrementally relinked with copied archived WRF objects to executable SHA-256 `48fd6b22dee7ab410d4fc66e76e8fb30bb8140ce80bbeef12e71fe8c12a24a90`; **this is not a clean full WRF rebuild**. The same reduced dry `em_b_wave` input and source/executable completed PC2 option-2 K=0/K=1000 and split-explicit RK3 K=1000 one-step runs. Each has two frames and 174 finite floating output fields; all three outputs are byte-identical to their respective #230 validated runs. A copied input with one nonzero QV cell was rejected with `requires zero QV`. The exact integration source/archive/executable/input/namelist/output hashes are in `.validation/integration/wrf/receipt.json`, SHA-256 `220ff3bb54a7a14e6452bf3090245afef9cdc6b17cb813f61ae6316d3273331d`.

The same-setup PC2–RK3 final-field RMS differences are inherited exactly from #230 because the output files are byte-identical: U `0.002088916`, V `0.0000220950`, W `0.0000873553`, PH `0.0325567`, T `0.0000312210`, MU `0.0414933` in their respective units. This is a one-step implementation comparison, not error against truth or same-accuracy performance. Longer 60/30/15/7.5 s output-field refinement did not demonstrate third-order convergence, and a stricter dt15 Newton tolerance stalled near scaled residual `1.325e-6`; those T1 results remain exploratory and are documented in the feature-branch temporal report.

### Fresh full WRF build and rerun (2026-09-25 17:24:40 JST)

Toolchain versions were GNU Fortran 15.2.0, OpenMPI 5.0.9, and Apple Clang 21.0.0.

The isolated integration checkout was then built from source with `./compile -j 4 em_b_wave`, using pinned MYNN submodule `90f36c25259ec1960b24325f5b29ac7c5adeac73`, GNU Fortran/OpenMPI and the same current production C++ archive. The target completed and generated both `main/ideal.exe` (SHA-256 `4a52a4406598e85c43301135d6745730916237e4a4ce79bc48c3c1332f7a403a`) and `main/wrf.exe` (SHA-256 `247fad5497c4d7b354bd0ef05fb061fe1bf8ad6d2ca906db75afb633f1e6450b`). `configure.wrf` SHA-256 is `e14c0ff55cee9df6b879b4c70948c6f281c9263f0d97ceca7053d3e1851d10ab`, compile log SHA-256 is `a77f3f06b5e54d2cac7c2c540c1ff2d24ca0dc9f3fad34c6385fa479c6844099`. The build log contains ignored `libSystem` link failures for auxiliary `test_io_idx`/`diffwrf` tools because `SDKROOT` was unset; the requested WRF executables linked successfully. This is a fresh WRF target build in the isolated checkout, not a proof that every auxiliary utility built.

With this freshly built executable, the identical archived input, `diagnostics.txt`, and namelists were used for PC2 option-2 `K=0`, PC2 option-2 `K=1000`, and RK3 option-2 `K=1000` one-step runs. All three completed with two frames and 174 finite floating fields. Each output is byte-identical to the earlier incrementally relinked counterpart, with respective SHA-256s `146f68669b51881c6d312daa5d0fa4961fe66e896398bb34bed5d511af8ff2d2`, `fa7ea6fd0764ad5d0fb6facd76222a8a59acbd859b648990466c1cfdde2bac7d`, and `96a3bbc23a4ebf93abc5221998b3d4995931f01ab6aadc7e4b648bfe2185ce46`. The copied nonzero-QV input was rejected before the native solve with `requires zero QV`. Fresh-run receipt `.validation/integration/wrf_fresh/receipt.json` has SHA-256 `b94274f669228dad71b7185765d6cc18af420fca5309aa1661646b412fbef7c9`; the run directory holds input, namelist, diagnostics, logs, and outputs. The first fresh-run attempt lacked `diagnostics.txt` and stopped at WRF input initialization; the completed comparison includes that required file.

The reported `Timing for main` for the single `K=1000` step was 0.41527 s for PC2 and 0.07716 s for RK3, a single-run ratio of 5.38. This includes the model step's normal work but does not establish equal-accuracy performance or reproducible benchmark timing.

## Remaining actions

This branch makes the reviewed candidate tree concrete for a `main`-target PR but does not close scientific qualification. The fresh `em_b_wave` target build and bounded one-step rerun are complete; general option-2 U/V/W stress and variable coefficient ownership, noncanonical/MPI boundaries, whole-step conservation/forcing time order, temporal third-order evidence, and a complete active full-step/observation adjoint remain open. The checked-in legacy WRFPLUS/Tapenade TL/AD routine has a pre-hybrid interface and cannot be made current by patching only the #226 V-Y terms. Keep the integration PR in draft while those scope choices and evidence are reviewed.
