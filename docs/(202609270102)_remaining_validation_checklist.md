# AD-SDIRK3 remaining validation after option-2 scalar stage-metric closure

Local timestamp: 2026-09-27 01:02:41 JST (+0900)

This checklist updates `(202609262338)_remaining_validation_checklist.md` with the bounded scalar stage-metric/sign candidate. The current isolated branch is based on #243 source HEAD `a1d5f6a3c8c64e66510c81d5fbe98ce568ed180f`; it is intended to stack on the #244 integration at `17b505c`. Local CTest, Make archive checks, and candidate-first one-step WRF relink checks are recorded in `(202609270058)_option2_scalar_stage_metric_closure.md`. Exact-head review/CI and full clean WRF rebuild of the final stack remain open.

| ID | Current status | Evidence and next closure condition |
|---|---|---|
| I0 integration | **Local candidate complete; #244 stack review/CI open** | The scalar candidate is isolated on #243 and has not been integrated. Cherry-pick onto #244 exact HEAD `17b505c`, review the complete stack, then run exact-head CI. |
| F1 option-1 scalar/momentum | **Signed scalar helper smoke passed; full parity open** | The actual option-1 vertical-only RHS smoke uses `khdif=0`, `kvdif=10`, flat unit maps, zero velocity, and monotone theta; the shared helper source-equation amplitude/sign control also passes. This is not full option-1 Fortran RHS parity. Existing option-1 momentum and scalar contracts remain in the focused test. |
| F1 option-2 scalar | **Current-stage metric and signed-eta closure passed for tested fixtures** | Same-state actual RHS ON−OFF comparisons match the source-equation oracle in physical and packed layouts for stress and simple branches. Old cached-rdz controls miss by 6.3e-8–9.7e-8 versus a 1.1e-10–1.2e-10 budget. Native `km_opt=1` fallback uses `xkhv=3*kvdif`; wrong-shaped explicit stage metrics fail closed. Dynamic/supplied K, moisture, other terrain/map/BC configurations, and longer forecasts remain unverified. |
| F1 option-2 U | **Composite operator parity for declared fixtures; full RHS open** | Existing U source-extracted composite fixture remains bounded evidence only. It is not `computeUnifiedRHS` full coupling or WRF halo/MPI parity. |
| F1 option-2 V | **Composite operator parity for declared fixtures; full RHS open** | Existing source-extracted V composite and controls remain passing on their prescribed terrain/packed states. Physical BC, MPI exchange, and complete RHS coupling remain open. |
| F1 option-2 W | **Composite operator parity for declared fixtures; full RHS open** | Existing source-extracted W composite and controls remain passing on their prescribed states. Physical BC, MPI exchange, and complete RHS coupling remain open. |
| F1 option-2 vertical U/V/W | **Prior bounded defect closure maintained** | PR #239 signed/mapped vertical U/V/W contracts pass on their prescribed fixtures; broader coupled diffusion parity remains open. |
| F1 top extrapolation `cfn/cfn1` | **Bounded production defect fixed in candidate** | Supplied WRF `fnm/fnp` with changed `rdnw` refreshes top coefficients on each zero-copy RHS call. External-to-internal weight-source switching remains unverified. |
| P1 one-step active wind blow-up | **Earlier bounded correction maintained; scalar candidate changes PC2 fields** | Final candidate PC2 is finite and completes 60 s, but has material one-step field deltas versus clean #244: max absolute U `3.34e-6`, V `1.84e-5`, W `3.082e-3`, PH `1.4693`, T `6.433e-2`, MU `1.812e-5`. RK3 remains bitwise identical. Do not claim forecast acceptance; investigate with Fortran full-RHS and longer-run comparisons. |
| S1b physical budgets | **Scoped existing coverage; quantitative endpoint closure open** | Accepted-stage face/source checks and replay exist. The bilinear omission mutants remain too weak relative to the experiment bound; no weak assertion was retained. Find a better-resolved fixture or justify the endpoint product-defect budget. |
| Reproducible full WRF | **Candidate-first relink verified; final clean full build open** | The one-step candidate executable links the scalar candidate archive in both library resolution positions with unchanged #244 Fortran objects/`libwrflib.a`. This is mixed-source validation, not a clean WRF build of the scalar candidate. Rebuild/relink the final #244-plus-scalar stack from exact HEAD. |
| G1 decomposition | **Open** | Need forward and transpose ownership/halo equivalence across multiple tile and MPI layouts. |
| K1 coefficient derivatives | **Open** | Fixed-K RHS derivatives exist; diagnosed K/rho policy and variable-coefficient stage JVP/VJP remain. |
| T1 time accuracy, stability, cost | **Open** | Existing PC2/RK3 refinement evidence is mixed/nonmonotone and does not establish third order or equal-accuracy speed. The new scalar change has only a one-step PC2/RK3 check. |
| A1 complete active-step adjoint | **Open** | Need current-geometry ARK-step derivative, physical-boundary/MPI transpose, and objective Taylor evidence; WRFPLUS TL/AD must match the current primal. |

Next closure: integrate onto #244 exact HEAD `17b505c`, obtain Red review and exact-head CI, and rerun the clean WRF build/relink. Then compare a fixed common option-2 state through the full U/V/W/scalar RHS with WRF halo/BC, map/mass coupling, and current geometry. Resolve the PC2 one-step field deltas before any forecast-quality claim. S1b, G1, K1, T1, and A1 remain open.
