# AD-SDIRK3 remaining validation checklist after clean full build

Local timestamp: 2026-09-26 10:03:36 JST (+0900)

This supersedes the status table in `(202609260859)_remaining_validation_checklist.md` for the exact PR #239 source commit `1c5c828470b858146ad6b8f8e539a1ee79dade46`. Draft PR #239 is stacked on #238, not merged into `main`. Its manual exact-head SDIRK3 CI run `36203953243` succeeded, including core CTest and source-extracted vertical U/V/W parity. This is a validation ledger, not operational forecast or data-assimilation acceptance.

| ID | Current status | Closure evidence and remaining condition |
|---|---|---|
| I0 branch integration | **Open** | Draft stack #233–#239 is reviewed and CI-tested in pieces; merge only after the final combined source and intended supported options are qualified. |
| F1 option-1 scalar/momentum | **Scoped candidate passed** | Existing source parity, packed cases and active fixed-K derivative contracts remain green. Other physics, boundaries, MPI, and regenerated WRFPLUS TL/AD are open. |
| F1 option-2 scalar | **Scoped candidate passed** | Dry constant-K `km_opt=1` stage metric and scalar manufactured cases pass. Dynamic or supplied K, moist states and general tile layouts remain unsupported or unverified. |
| F1 option-2 horizontal U/V/W | **Open** | Bounded U producer-chain and W sign oracles exist. Same-state composite U now has a promising local fixture but its first top-pulse mismatch is still under investigation; full U/V/W terrain/boundary parity is not closed. |
| F1 option-2 vertical U/V/W | **Bounded defect closed** | U/V all mass layers (4,352 values each) and W stress (4,335 points) match compiled Fortran in prescribed-state fixtures; K=0 and old-path negative controls discriminate the repaired terms. Full active production RHS parity remains open. |
| P1 one-step active wind blow-up | **Resolved in tested dry case** | The stage-metric fix reduced same-source PC2–RK3 U RMS difference from ~29,807 to 0.00209013 m/s at `khdif=1000,kvdif=10`, 60 s. This is not a predeclared forecast-error acceptance. |
| S1b weighted whole-step and accepted-stage budgets | **Scoped existing coverage verified; quantitative endpoint closure open** | At this HEAD, `test_full_tile_step.cpp` already checks nonunit map area, nonuniform hybrid layer mass, a wrong `/nz` counterexample, accepted-stage face/tendency and physical dry-mass/theta rates, closed-boundary flux, and exact raw/projected ARK replay. The nonlinear endpoint (M\theta) minus stage chain-rule sum is only finite-checked. Independent expansion matched its reported −9703.17 with residual 5.09e−9, but plausible one-term omissions produced only ~2.07× the experiment's bound, below the predeclared 100× discriminator; no weak assertion was added. Next use a better-resolved fixture or a separately justified product-defect error budget. |
| G1 decomposition | **Open** | Need two tile layouts plus MPI halo/ownership and transpose equivalence for the supported operator. |
| K1 coefficient derivatives | **Open** | Fixed-K RHS VJP exists; diagnosed K/rho dependence and variable-coefficient stage JVP/VJP policy remain to be declared and checked. |
| T1 time accuracy/stability/cost | **Open** | Clean one-step PC2/RK3 fields are close and finite, but no third-order or long-run acceptance follows. Resolve prior solver/output refinement floor; repeat at equal accuracy and longer duration. |
| A1 complete active-step adjoint | **Open** | Need full ARK-step derivative including current-state geometry/density, packing, boundary/halo transpose and objective Taylor test; current fixed-context RHS and limited converged-stage checks do not establish it. |
| Reproducible full WRF build and ideal initialization | **Closed for this exact macOS source/configuration** | Fresh detached checkout at `1c5c828`, pinned MYNN gitlink, `./clean -a`, SDKROOT configure and `./compile em_b_wave -j 8` produced new `ideal.exe` and `wrf.exe`. New `ideal.exe` regenerated the archived `wrfinput_d01` byte-identically (SHA-256 `e71b730a…`). Clean-built PC2/RK3 using the exact archived input/namelists each completed a one-minute `khdif=1000,kvdif=10` run, 174 finite float variables. Clean RK3 output is byte-identical to the earlier incremental output; PC2 differs only at small FP32 levels in reported fields. Other compilers/platforms and MPI layouts remain untested. |

Evidence: `(202609260958)_pr239_clean_full_wrf_validation.md`, `(202609260935)_S1b_weighted_budget_review.md`, `(202609260949)_S1b_ark_product_identity_findings.md`, and PR #239's operator report. The clean build uses gfortran 15.2, Apple clang 21, OpenMPI 5.0.9, netCDF 4.9.3/Fortran 4.6.2, Homebrew LibTorch 2.10 ABI0. The clean-build one-step WRF main times were 0.44935 s PC2 and 0.07419 s RK3; these are single measurements, not equal-accuracy performance evidence. Detailed source, executable, input, output and log hashes are in the clean-build report.

Next work should resolve the first same-state option-2 U horizontal top-pulse discrepancy rather than selecting only a passing fixture; then extend the common-state Fortran comparison through V/W/scalar and map/mass coupling. After that, revisit physically consistent time refinement, decomposition, and the actual active-step adjoint. No model run or RK3 comparison was performed by the S1b documentation-only analysis itself; those measurements come from the separate clean-build validation.
